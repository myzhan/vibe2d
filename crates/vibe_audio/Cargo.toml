[package]
name = "vibe_audio"
version.workspace = true
edition.workspace = true
license.workspace = true

[dependencies]
anyhow.workspace = true
tracing.workspace = true
vibe_asset = { workspace = true }

[target.'cfg(not(target_arch = "wasm32"))'.dependencies]
rodio.workspace = true

[target.'cfg(target_arch = "wasm32")'.dependencies]
web-sys = { workspace = true, features = [
    "AudioContext", "AudioBuffer", "AudioBufferSourceNode",
    "AudioDestinationNode", "BaseAudioContext",
    # Music channel needs a gain node it can retain, so volume and the
    # pause-by-muting path can address it after playback has started.
    "GainNode", "AudioNode", "AudioParam",
] }
wasm-bindgen.workspace = true
wasm-bindgen-futures.workspace = true
js-sys.workspace = true
